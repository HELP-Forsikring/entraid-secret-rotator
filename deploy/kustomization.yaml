---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cronjob.yaml
  - namespace.yaml
  - prometheusrules.yaml
  - serviceaccount.yaml

images:
  - name: "replace/me"
    newName: "your.registry.io/your-repo"
    newTag: "x.y.z"
